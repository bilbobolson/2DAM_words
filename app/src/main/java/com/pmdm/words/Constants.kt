package com.pmdm.words

object Constants
{
    const val INTENTOS: Int = 6           // número de intentos (filas)
    const val LETRAS_PALABRA: Int = 5     // número de letras por palabra (columnas)

    const val H: String = "#2ec93b"       // color del acierto (hits)
    const val C: String = "#c9aa2e"       // color de coincidencia (coincidence)
    const val F: String = "#bfbfbf"       // color del fallo (fail)
    const val V: String = "#ffffff"       // color vacío (void)

    const val MARK:String = "."           // marcado de letra examinada
}